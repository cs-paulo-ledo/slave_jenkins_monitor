1- Capturar o json
saida_json_url = requests.get('http://ci.knowledge4.life:8080/computer/api/json')

2- enviar um dicionario para uma variavel
obj_json_request = saida_json_url.json()


#-----------------------------
3- Enviar a uma lista com todos os hosts envolvidos no CI (master e slaves)
nodes_slave = obj_json_request.get('computer')

nodes_slave : lista com todos os hosts envolvidos, o primeiro é o master
* posso remover o master da lista


* ou posso apenas iterar na lista assim:
lista_hosts[1]['displayName']
lista_hosts[1]['offline']

4- Iterar na lista, apartir do elemento 1 que são os slaves.
knowledge_njr_android_slave = nodes_slave[1]

5- Verificar essa cha, se estiver True, o slave estar fora, devemos subir.
knowledge_njr_android_slave['offline']

